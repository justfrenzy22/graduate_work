const NoCopyrightText = {
    bg: {
        noCopyRight: `Този сайт не съхранява никакви файлове на нашия сървър, ние само свързваме с 3-та партньорска услуга.`
    } as { noCopyRight: string },
    en: {
        noCopyRight: `This site does not store any files on our server, we only linked to the media which is hosted on 3rd party services.`
    } as { noCopyRight: string }
}

export default NoCopyrightText;